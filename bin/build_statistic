#!/usr/bin/php
<?php
require('../inc/build_statistic.php');

$stat = build_statistic();

$template_count = $stat[''];
foreach ($stat as $lang => $count) {
  if ($lang !== '') {
    printf("%s\t%d\t%.2f\n", $lang, $count, $count / $template_count * 100);
  }
}
